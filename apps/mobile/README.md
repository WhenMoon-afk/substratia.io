# @substratia/mobile

**Status: Parked** — This app is not actively developed.

The mobile app is excluded from default dev and build commands. To work on it explicitly:

```bash
bun run dev:mobile
```
